package com.sparta.deliverit.ai.domain.repository;

import com.sparta.deliverit.ai.domain.entity.AiMenuDescription;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AiMenuDescriptionRepository extends JpaRepository<AiMenuDescription, String> {
}
